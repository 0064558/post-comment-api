package com.example.postcommentapi;

/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.mongodb.core.MongoTemplate;

@Configuration
public class TestConfig implements CommandLineRunner {

    @Autowired
    private MongoTemplate mongoTemplate;

    @Override
    public void run(String... args) throws Exception {
        System.out.println("\n=== DIAGNÓSTICO MONGODB ===");
        System.out.println("Banco de dados conectado: " + mongoTemplate.getDb().getName());
        System.out.println("Coleções existentes nesse banco: " + mongoTemplate.getCollectionNames());

        String nomeColecao = "user";

        long count = mongoTemplate.getCollection(nomeColecao).countDocuments();
        System.out.println("Total de documentos na coleção '" + nomeColecao + "': " + count);
        System.out.println("===========================\n");
    }
}*/
